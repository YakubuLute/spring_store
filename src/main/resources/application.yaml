spring:
  application:
    name: CreatorStore
# Database Information

  datasource:
    url:
    username:
    password:

  # ORM Configuration
  # Hibernate - ORM for Java configuration
  jpa: # JPA - Jakarta
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true